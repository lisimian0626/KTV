<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--&lt;!&ndash;  设置背景色图像资源  &ndash;&gt;-->
    <!--<item-->
    <!--android:id="@android:id/background"-->
    <!--android:drawable="@drawable/pgb_bg" />-->
    <!--&lt;!&ndash;  设置进度条颜色图像资源  &ndash;&gt;-->
    <!--<item-->
    <!--android:id="@android:id/secondaryProgress"-->
    <!--android:drawable="@android:color/transparent" />-->
    <!--&lt;!&ndash;  设置进度条颜色图像资源  &ndash;&gt;-->

    <!--<item-->
    <!--android:id="@android:id/progress"-->
    <!--android:drawable="@drawable/pgb_progress" />-->

    <!-- 背景  gradient是渐变,corners定义的是圆角 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />
            <solid android:color="#A47034" />
        </shape>
    </item>
    <!-- 第二条进度条颜色 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <gradient
                    android:angle="90.0"
                    android:centerColor="#FFF960"
                    android:centerY="0.45"
                    android:endColor="#FFF960"
                    android:startColor="#FFF960" />
            </shape>
        </clip>
    </item>
    <!-- 进度条 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <solid android:color="#FFF960" />
            </shape>
        </clip>
    </item>

</layer-list>
